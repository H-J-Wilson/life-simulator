1. Download project files in a folder
2. Using cd in the command line change to its stored directory
3. Run "pip install fastapi uvicorn requests" on the machine hosting
4. Run "python main.py"
5. Run "hostname -I" to get your ip address
6. open the webpage with "http://your.ip.adresss:8000"
